<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns:fx="http://javafx.com/fxml" fx:controller="GUI.controllers.UsuariosController" spacing="8">
    <padding>
        <Insets bottom="10.0" left="10.0" right="10.0" top="10.0" />
    </padding>
    <ToolBar>
        <Button onAction="#goBack" text="Voltar" />
        <Label styleClass="title" text="Usuários" />
    </ToolBar>

    <TableView fx:id="usuariosTable">
        <columns>
            <TableColumn fx:id="colUserId" text="ID" />
            <TableColumn fx:id="colUserNome" text="Nome" />
            <TableColumn fx:id="colUserEmail" text="E-mail" />
            <TableColumn fx:id="colUserRole" text="Role" />
        </columns>
    </TableView>

    <HBox spacing="8">
        <Button onAction="#novoUsuario" text="Novo" />
        <Button onAction="#editarUsuario" text="Editar" />
        <Button onAction="#excluirUsuario" text="Excluir" />
    </HBox>
</VBox>
